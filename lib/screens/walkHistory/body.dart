import 'package:flutter/material.dart';

class WalkHistoryBody extends StatelessWidget {
  const WalkHistoryBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      constraints: BoxConstraints.expand(height: 450),
    );
  }
}
